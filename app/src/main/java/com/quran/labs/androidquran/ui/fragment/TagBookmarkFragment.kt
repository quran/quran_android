package com.quran.labs.androidquran.ui.fragment

import androidx.fragment.app.Fragment

/**
 * Compatibility shim for restoring pre-upgrade sliding panel state.
 */
@Deprecated("Only retained so old FragmentManager state can be discarded after upgrade.")
class TagBookmarkFragment : Fragment()
